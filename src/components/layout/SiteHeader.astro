---
import { MobileNav } from '../interactive/MobileNav.tsx';

const links = [
  { href: '/work', label: 'Work' },
  { href: '/services', label: 'Services' },
  { href: '/about', label: 'About' },
  { href: '/contact', label: 'Contact' },
];
---

<header class="sticky top-0 z-50 w-full glass-surface">
  <div class="container mx-auto flex h-16 items-center justify-between px-4 md:px-8">
    <a href="/" class="text-lg font-bold tracking-tight hover:opacity-80 transition-opacity">
      <span class="text-primary">P</span><span class="text-foreground">radnya.</span>
    </a>

    <!-- Desktop Nav -->
    <nav class="hidden md:flex items-center gap-8">
      {links.map((link) => (
        <a href={link.href} class="text-sm font-medium text-muted-foreground hover:text-foreground transition-colors tracking-wide">
          {link.label}
        </a>
      ))}
      <a href="/contact" class="inline-flex h-9 items-center justify-center rounded-full bg-primary px-4 py-2 text-sm font-medium text-primary-foreground transition-transform hover:bg-primary/90 focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-primary">
        Discuss Your Project
      </a>
    </nav>

    <!-- Mobile Nav Toggle (React Island) -->
    <MobileNav client:load />
  </div>
</header>
